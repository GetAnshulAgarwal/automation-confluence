apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: metrics-access
rules:
  - apiGroups:
      - "metrics.eks.amazonaws.com"
    resources:
      - "kcm/metrics"
      - "ksh/metrics"
    verbs:
      - "get"
