environments:
  uat:
    values:
      - ecrRegistry: 381491900351.dkr.ecr.ap-southeast-1.amazonaws.com
  prd:
    values:
      - ecrRegistry: 654654228951.dkr.ecr.ap-southeast-1.amazonaws.com
---
bases:
  - ../../global.yaml
  - ../app.yaml

releases:
  - name: gatekeeper
    installed: true
    namespace: gatekeeper
    chart: ecr-uat-helm-charts/gatekeeper
    version: 3.21.1
    hooks:
      - events: ["presync"]
        showlogs: true
        command: "kubectl"
        args:
          - "apply"
          - "-f"
          - "namespace.yaml"
          - "--context"
          - "{{.Environment.KubeContext}}"
    values:
      # - default.values.yaml
      - "{{ .Environment.Name }}.values.yaml"
