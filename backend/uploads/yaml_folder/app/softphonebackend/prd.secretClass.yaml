apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: softphonebackend-spc # keep -spc suffix
  namespace: softphonebackend
spec:
  provider: aws
  parameters:
    objects: |
      - objectName: "AmazonMSK_PRD-SECRET-KAFKA-SOFTPHONEBACKEND"
        objectType: "secretsmanager"
        jmesPath:
          - path: password
            objectAlias: KAFKA_PASSWORD-alias
      - objectName: "PRD-KEYCLOAK-CLIENT-SOFTPHONEBACKEND"
        objectType: "secretsmanager"
        jmesPath:
          - path: client_secret
            objectAlias: KEYCLOAK_CLIENT_SECRET-alias
      - objectName: "PRD-APP-MONGO-LIO"
        objectType: "secretsmanager"
        jmesPath:
          - path: password
            objectAlias: MONGO_DB_PASS-alias
      - objectName: "PRD-EC2-ASTERISK"
        objectType: "secretsmanager"
        jmesPath:
          - path: AMISERVER_PASSWORD
            objectAlias: AMISERVER_PASSWORD-alias
  secretObjects: # using secret objects require syncSecret.enabled=true else you will get configuration error during deployement
    - data:
        - key: KAFKA_PASSWORD # key whose corresponding value will have content of the secret
          objectName: KAFKA_PASSWORD-alias # name of the mounted content to sync. this could be the object name or the object alias
        - key: KEYCLOAK_CLIENT_SECRET # key whose corresponding value will have content of the secret
          objectName: KEYCLOAK_CLIENT_SECRET-alias # name of the mounted content to sync. this could be the object name or the object alias
        - key: MONGO_DB_PASS # key whose corresponding value will have content of the secret
          objectName: MONGO_DB_PASS-alias # name of the mounted content to sync. this could be the object name or the object alias
        - key: AMISERVER_PASSWORD # key whose corresponding value will have content of the secret
          objectName: AMISERVER_PASSWORD-alias # name of the mounted content to sync. this could be the object name or the object alias
      secretName: aws-secrets-manager-secret # DO NOT CHANGE THIS
      type: Opaque